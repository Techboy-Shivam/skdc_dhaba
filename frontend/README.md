# React + Vite

This template provides a minimal setup to get React working in Vite with HMR and some ESLint rules.

Currently, two official plugins are available:

- [@vitejs/plugin-react](https://github.com/vitejs/vite-plugin-react/blob/main/packages/plugin-react/README.md) uses [Babel](https://babeljs.io/) for Fast Refresh
- [@vitejs/plugin-react-swc](https://github.com/vitejs/vite-plugin-react-swc) uses [SWC](https://swc.rs/) for Fast Refresh

✅Update teh database from here if update thhe admin panel in admin.jsx only regflect in db
✅Name Enter in blank taking as correct 
After booking same person at same time its show poup to already you booked a table
Same person can book table a diff time
Pay 200 fer addvanced booking
Add hotel location and also Imges
Add payment at their show a message to sent mobile number or email urs booking succesfull
Add qr code to payment
One gmail not book multiple table popup is there not allowed
Pay Now button to request ₹199 for table booking confirmation